Run the NaliBot setup wizard. This installs all required packages, walks you through entering your Kraken API key and Telegram credentials, tests that Telegram is working, and trains the ML model. Run this once before anything else.

```bash
python setup.py
```
